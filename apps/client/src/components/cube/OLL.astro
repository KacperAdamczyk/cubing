---
import { Colors } from "cube";
import LastLayer from "@/components/cube/LastLayer.astro";

interface Props {
	algorithm: string;
}

const { algorithm } = Astro.props;
---

<LastLayer
  maskedColors={[Colors.B, Colors.G, Colors.O, Colors.R, Colors.W]}
  algorithm={algorithm}
/>
